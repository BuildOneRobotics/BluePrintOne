<Window x:Class="BluePrintOne.CadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CAD Design - BluePrintOne" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen" Background="#F5F5F5">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#6B46C1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E0E0E0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0" Background="#6B46C1">
            <StackPanel Orientation="Horizontal" Margin="15,10">
                <Button Content="✏️ Line" Width="80" Margin="5" Click="Line_Click"/>
                <Button Content="▭ Rectangle" Width="100" Margin="5" Click="Rectangle_Click"/>
                <Button Content="⭕ Circle" Width="80" Margin="5" Click="Circle_Click"/>
                <Button Content="🗑️ Clear" Width="80" Margin="5" Click="Clear_Click"/>
            </StackPanel>
        </Border>
        
        <Border Grid.Row="1" Background="White" Margin="10" BorderBrush="#CCCCCC" BorderThickness="1">
            <Canvas x:Name="DrawCanvas" Background="White" 
                    MouseLeftButtonDown="Canvas_MouseDown" MouseMove="Canvas_MouseMove" MouseLeftButtonUp="Canvas_MouseUp"/>
        </Border>
    </Grid>
</Window>
